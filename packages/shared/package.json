{
  "name": "@nodetunnel/shared",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "前后端共用的类型、常量与校验规则",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo \"@nodetunnel/shared 由使用方直接编译 TS 源码，无需构建\"",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
