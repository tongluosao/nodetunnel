{
  "name": "@nodetunnel/protocol",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "EasyTier 配置渲染（TOML + ACL）与配置服务器协议",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "build": "echo \"@nodetunnel/protocol 由使用方直接编译 TS 源码，无需构建\"",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@nodetunnel/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.3",
    "vitest": "^3.0.0"
  }
}
