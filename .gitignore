# 依赖
node_modules/
.pnpm-store/

# 构建产物
dist/
build/
.turbo/
*.tsbuildinfo

# WASM 生成物与 Cargo 构建缓存
# 内核已于 0.2.0 移除。保留这条是为了让历史工作树里可能残留的目录
# 不会被误提交（旧版本用它把 cargo 的 target 重定向出参考目录）。
.wasm-build/

# Cloudflare / Wrangler
.wrangler/
.dev.vars
.dev.vars.*
!.dev.vars.example

# 测试与覆盖率
coverage/
.nyc_output/

# 编辑器与系统
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db

# 日志
*.log
logs/

# 参考代码（只读参考，绝不入库、绝不修改）
其他项目代码/
